# Web3 黑话词典

一个专为 X (Twitter) 社区打造的 Web3 黑话词典，让用户能够快速查询、理解并分享 Web3 术语。

## 功能特性

### 核心功能 ✅
- **智能搜索**：支持中英文双语搜索，模糊匹配
- **分类浏览**：按主题分类浏览（入圈必看、基础概念、交易、DeFi 等）
- **术语详情**：显示英文全称、中文通俗解释、典型用法
- **响应式设计**：移动端优先，完美适配各种屏幕尺寸
- **暗黑模式**：内置 Dark Mode 支持

### 分享功能 📤
- **Twitter 分享**：一键分享术语到 X (Twitter)
- **动态 OG 图片**：自动生成精美的分享卡片
- **Twitter Card 优化**：确保分享链接显示预览卡片

### 社区功能 👥
- **用户认证**：支持 GitHub、Google OAuth 登录（需配置 Supabase）
- **用户贡献**：提交新术语供审核（需登录）
- **打赏系统**：支持 Lightning Network、EVM、Solana（UI 演示）
- **版权保护**：CC BY-NC-SA 4.0 协议授权

## 技术栈

- **框架**：Next.js 16 (App Router)
- **语言**：TypeScript
- **样式**：Tailwind CSS
- **图标**：Lucide React
- **部署**：Vercel (推荐)

## 项目结构

```
web3-glossary/
├── src/
│   ├── app/                    # Next.js App Router 页面
│   │   ├── [slug]/            # 单个术语详情页
│   │   │   └── page.tsx       # 包含动态 metadata
│   │   ├── api/og/            # OG 图片生成 API
│   │   │   └── [slug]/route.tsx
│   │   ├── layout.tsx          # 根布局
│   │   ├── page.tsx            # 首页
│   │   └── globals.css         # 全局样式
│   ├── components/             # React 组件
│   │   ├── TermCard.tsx        # 术语卡片
│   │   ├── SearchBox.tsx       # 搜索框
│   │   ├── CategoryNav.tsx     # 分类导航
│   │   ├── ContributionForm.tsx # 贡献表单
│   │   └── TippingModal.tsx   # 打赏模态框
│   ├── data/                  # 数据文件
│   │   └── terms.ts           # 术语和分类数据
│   └── lib/                   # 工具函数
│       ├── types.ts            # TypeScript 类型定义
│       └── utils.ts           # 搜索等工具函数
├── public/                     # 静态资源
├── package.json
├── tsconfig.json
├── tailwind.config.ts
└── next.config.ts
```

## 快速开始

### 前置要求

- Node.js >= 20.9.0（推荐）或 >= 18.18.0
- npm, yarn, pnpm, 或 bun

### 安装依赖

```bash
npm install
```

### 运行开发服务器

```bash
npm run dev
```

打开 [http://localhost:3000](http://localhost:3000) 查看结果。

### 构建生产版本

```bash
npm run build
npm start
```

## 数据管理

### 添加新术语

编辑 `src/data/terms.ts` 文件，在 `terms` 数组中添加新术语：

```typescript
{
  id: 'unique-id',
  term: 'HODL',
  englishFull: 'Hold On for Dear Life',
  chinese: '最早拼写错误的 hold，现在特指"死拿不卖"',
  examples: ['HODL your BTC', 'stay strong, wagmi'],
  categoryIds: ['essentials', 'trading'],
  slug: 'hodl',
  createdAt: new Date().toISOString(),
  viewCount: 0,
  shareCount: 0,
}
```

### 添加新分类

编辑 `src/data/terms.ts` 文件，在 `categories` 数组中添加新分类：

```typescript
{
  id: 'new-category',
  name: '新分类',
  icon: '📌',
  sortOrder: 10,
}
```

## 认证配置（可选）

### 使用 Supabase 设置用户认证

项目已包含完整的 Supabase 认证代码，如需启用用户功能：

1. **配置 Supabase 项目**
   - 参考 [SUPABASE_SETUP.md](./SUPABASE_SETUP.md) 详细指南
   - 创建数据库表
   - 配置 OAuth 提供者（GitHub、Google）

2. **配置环境变量**
   ```bash
   # 复制示例文件
   cp .env.local.example .env.local

   # 编辑 .env.local 添加你的 Supabase 凭证
   NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
   ```

3. **认证功能**
   - GitHub/Google OAuth 登录
   - 用户资料管理
   - 贡献历史追踪
   - 打赏记录

**注意**：当前项目使用静态数据（`src/data/terms.ts`），认证功能为代码框架，需要配置 Supabase 后才能完全使用。

## 部署

### Vercel 部署

1. 将代码推送到 GitHub
2. 在 [Vercel](https://vercel.com) 导入项目
3. Vercel 会自动检测 Next.js 并配置构建

### 其他平台

本项目是标准的 Next.js 应用，可以部署到任何支持 Node.js 的平台：
- Netlify
- AWS Amplify
- Cloudflare Pages
- Railway
- Render

## 未来计划

- [ ] 集成 Supabase 实现用户认证
- [ ] 实现真实的数据后端
- [ ] 添加术语收藏功能
- [ ] 实现真正的钱包连接和打赏
- [ ] 添加多语言支持
- [ ] 实现 PWA 离线访问
- [ ] 添加搜索历史记录

## 贡献

欢迎提交 Issue 和 Pull Request！

## 许可证

MIT
